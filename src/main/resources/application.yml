server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/todo_app_db?useSSL=false&serverTimezone=UTC
    username: root
    password: 123456789

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  jackson:
    serialization:
      INDENT_OUTPUT: true
